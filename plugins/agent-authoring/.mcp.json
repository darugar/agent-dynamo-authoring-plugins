{
  "mcpServers": {
    "agent-dynamo": {
      "type": "http",
      "url": "${AGENTDYNAMO_MCP_URL:-https://app.agentdynamo.com/mcp}",
      "headers": {
        "Authorization": "Bearer ${AGENTDYNAMO_API_KEY}"
      }
    }
  }
}
